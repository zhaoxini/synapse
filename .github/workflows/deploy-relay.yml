# Deploy synapse-relay to the public VPS (zx0623.duckdns.org).
#
# Secrets: RELAY_SSH (e.g. root@192.3.179.202), RELAY_SSH_KEY

name: Deploy Relay

on:
  workflow_dispatch:
  push:
    branches: [master, main]
    paths:
      - "crates/relay/**"
      - "scripts/deploy-relay.sh"
      - "scripts/vps-upgrade-relay.sh"
      - ".github/workflows/deploy-relay.yml"

jobs:
  deploy:
    name: upgrade synapse-relay on VPS
    runs-on: ubuntu-latest
    continue-on-error: true
    steps:
      - uses: actions/checkout@v4

      - name: Install Rust toolchain
        uses: dtolnay/rust-toolchain@stable
        with:
          targets: x86_64-unknown-linux-gnu

      - name: Build synapse-relay (Linux x86_64)
        run: cargo build --release -p synapse-relay --target x86_64-unknown-linux-gnu

      - name: Upload binary and restart service
        env:
          RELAY_SSH: ${{ secrets.RELAY_SSH }}
          RELAY_SSH_KEY: ${{ secrets.RELAY_SSH_KEY }}
        run: |
          if [ -z "${RELAY_SSH_KEY:-}" ] || [ -z "${RELAY_SSH:-}" ]; then
            echo "RELAY_SSH / RELAY_SSH_KEY not configured — skip relay deploy"
            exit 0
          fi
          mkdir -p ~/.ssh
          printf '%s\n' "$RELAY_SSH_KEY" > ~/.ssh/relay_key
          chmod 600 ~/.ssh/relay_key
          host="${RELAY_SSH#*@}"
          ssh-keyscan -H "$host" >> ~/.ssh/known_hosts 2>/dev/null || true
          BIN=target/x86_64-unknown-linux-gnu/release/synapse-relay
          test -x "$BIN"
          scp -i ~/.ssh/relay_key -o StrictHostKeyChecking=accept-new \
            scripts/vps-upgrade-relay.sh \
            "$BIN" \
            "${RELAY_SSH}:/tmp/vps-upgrade-relay.sh" \
            "${RELAY_SSH}:/tmp/synapse-relay.new"
          ssh -i ~/.ssh/relay_key -o StrictHostKeyChecking=accept-new "${RELAY_SSH}" '
            set -e
            chmod +x /tmp/vps-upgrade-relay.sh
            bash /tmp/vps-upgrade-relay.sh /tmp/synapse-relay.new
            rm -f /tmp/synapse-relay.new /tmp/vps-upgrade-relay.sh
          '

      - name: Verify pairing exchange API
        continue-on-error: true
        run: |
          body="$(curl -s -X POST \
            'https://zx0623.duckdns.org/api/v1/pairing-codes/exchange' \
            -H 'Content-Type: application/json' \
            -d '{"code":"000000"}')"
          echo "${body}"
          if echo "${body}" | grep -q 'invalid or expired pairing code'; then
            echo "exchange API live"
            exit 0
          fi
          echo "exchange API not deployed yet (skip — RELAY_SSH_KEY may be unset)"
          exit 0
